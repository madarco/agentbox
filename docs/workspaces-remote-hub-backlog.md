# Remote control box — backlog

Host-local assumptions in the 0.31.x features that break or degrade when the hub is a remote control
box (`relay.controlPlaneUrl` configured). Found by the 2026-09-15 audit that produced
[`workspaces-remote-hub-plan.md`](./workspaces-remote-hub-plan.md); that plan covers only the 0.32
workspace layer and deliberately leaves these alone. Each item is documented, not fixed. Size:
S < half a day, M ≈ a day.

| # | Feature | Remote-hub behaviour | Where | Size |
|---|---|---|---|---|
| ~~1~~ | ~~`--model-auth` on a hub-routed create~~ | **Fixed 2026-09-19.** Was: the flag was never sent, so a hub-routed box booted with no model auth. Fixed on the principle **ship the SELECTION, not the secret** — the bytes already reach the control box (`syncAgentCredentialsIfChanged` before every hub create, then `seedHostBackupsFromCustody` immediately before `provider.create`, and `resolveHostCredentialFile` prefers that backup over the VPS's own home), so only the chosen ids were missing. `CreateJobRequestOpts` gained `borrowCredentials`, `controlPlaneCreateRequest` forwards it, and the worker passes it into `mod.provider.create`. All four CLI drop sites now carry it: the foreground and `-i` hub shapes (one `hubCreateOpts` helper), the LOCAL `-i` queue — whose gate was hoisted into a memoised closure called from both branches, since the `-i` block returned before it ever ran — and `POST /api/v1/boxes` from `agentbox create`. **Phase 5b landed with it**: `agentbox create --model-auth <source...>` on an agentless box (explicit-only, legal ids = the `agent` sources the registry declares, `env:` and undeclared ids refused; the seed is recorded on the box so an agent started there LATER runs the hash-gated ingest at its first start seam). Guard: `apps/hub/test/control-plane-create.test.ts` (the third field that mapping has swallowed), `apps/cli/test/model-auth-create-routing.test.ts`. | `apps/cli/src/commands/_cloud-agent-via-hub.ts`, `apps/cli/src/commands/create.ts`, `apps/cli/src/agents/command/create-action.ts`, `apps/cli/src/lib/model-auth-gate.ts`, `packages/relay/src/store/store.ts`, `apps/hub/lib/boxes/control-plane-create.ts`, `apps/hub/lib/hub-worker.ts` | — |
| ~~2~~ | ~~`agentbox <service-agent>` create (openclaw)~~ | **Fixed 2026-09-19.** Was: `provider.create` inline, no routing, no docker gate, and a request missing `controlPlaneUrl`/`gitPushMode`/`hubGitAuth`/`providerOptions` — so a cloud bot never registered with the control box and `agentbox openclaw` built a local docker box on a machine where `agentbox claude` refused one. The create path now runs `dockerProviderRefusal` + `localDockerUnsupportedWarning` before anything is built, resolves the provider NAME once up front, keeps the `persistentRefusal` ahead of routing (so e2b/vercel are refused in one line rather than as a hub job that fails after provisioning), and routes through `resolveCreateRouting` → `createCloudBoxViaHubAndAdopt` — the COLD `startAgent: false` shape, since a service agent has no session and no prompt (the worker already resolves the agent and gates the session on `createStartsSession`). The ending is unchanged: `ensureAgentInstalled` → `waitForService` → print the URL. `--restore` stays local by design (its workspace is a staged bundle on this machine). `nameBasis` needed no CLI change — the worker derives it from the repo URL itself. Guard: `apps/cli/test/service-create-routing.test.ts`. **Known gap, now more visible:** per-box channel tokens, item 21. | `apps/cli/src/agents/command/service-action.ts` (create path) | — |
| 3 | `agentbox git push/pull/checkout/branch` on a docker box | **Won't fix — a consequence of an unsupported combination (2026-09-19).** `withHubClient({})` with no `boxOwningHubIsLocal`, so with a control box configured the op goes to the remote hub, which never owned the box (`not_found`). **Live-verified 2026-09-18 (§G)**: ALL FIVE mutating ops fail identically with `box <id> not found` (exit 2) — `git push`, `git push --host-only`, `git pull`, `git checkout`, `git branch` — while `git status` (no `withHubClient`) works, which isolates the cause. `withOwningHub` already exists in `with-hub.ts` and is simply not called from `git.ts`. But a docker box can only exist next to a control box via `hub.mode=local`, and local docker alongside a control box is **not supported**: the refusals no longer recommend that mode and every create/bake under it warns, naming this failure. Reopen only if the combination is ever supported. | `apps/cli/src/commands/git.ts` vs `apps/cli/src/control-plane/with-hub.ts` (`boxOwningHubIsLocal`) | — |
| 4 | `download --backup` / `--restore` | PC-side, and diverges from the hub's own `POST /boxes/{id}/backup` store (two machines' disks, `GET /projects/{id}/bots` lists only the hub's). A hub-created cloud box with no local checkout resolves `projectRoot` to the literal `/workspace`, so the backup dir becomes `/workspace/.agentbox/bots/…` at the Mac's filesystem root. **Live 2026-09-18 (§G): the more common shape is worse and has a second symptom.** A hub-created cloud box's `projectRoot` is the worker's throwaway clone dir on the VPS (`/tmp/agentbox-hub-worker-<uuid>`), which the worker DELETES. Because `hasGit` is `hasGitAt(projectRoot)` on the hub's disk (`hub-backend.ts:527,544`), every hub-created cloud box therefore reports **`hasGit: false`** on `GET /api/v1/boxes` even after it has demonstrably committed and pushed — and per `bot-lifecycle-ui-plan.md` the tray and web UI hide git actions on `hasGit: false`. `agentbox clone` also PRINTS that dead path as the clone's workspace (`workspace: /tmp/agentbox-hub-worker-…/.agentbox/bots/<name>/workspace`), a location that exists on neither machine. | `apps/cli/src/commands/download.ts`, `packages/sandbox-core/src/sync/concerns/bot-backup.ts`, `apps/cli/src/control-plane/hub-adopt.ts` (`matchLocalProject`), `packages/relay/src/registration-to-record.ts` | S |
| 5 | `agentbox clone` per-box secrets | Correct routing, but `~/.agentbox/openclaw/<name>.env` is resolved with the HUB's `homedir()`, and the refusal names a path on a machine the user is not on. **Live 2026-09-18 (§G): not only the refusal** — a SUCCESSFUL `agentbox clone` prints the same kind of hub-side path (see item 4). | `apps/hub/lib/hub-backend.ts` (`clonePerBoxCarryRefusal`), `packages/sandbox-core/src/per-box-carry.ts` | S |
| 6 | `POST /api/v1/projects` (mkdir + git init) | Creates the folder on the hub; the web UI copy says "Pick a folder on this machine". | `apps/hub/lib/boxes/create-project.ts`, `apps/hub/app/(dashboard)/boxes/components/add-project-modal.tsx` | S |
| 7 | `POST /boxes/{id}/open` on a `hub expose`-d Mac | `canOpenInHostApps()` gates on `hubProfile() === 'localhost'`; an exposed Mac reports the hetzner profile while being the user's machine, so open-in is refused for no reason. Should use the `onThisMachine` distinction. | `apps/hub/lib/hub-backend.ts` (`canOpenInHostApps`) | S |
| ~~8~~ | ~~`doctor --json`~~ | **Fixed 2026-09-19.** Was: returns before `renderControlBoxProviders()`, so the tray's provider/bake rows describe the laptop while cloud boxes build from the control box's bakes — **live-verified 2026-09-18 (§G)**: same machine, same moment, `doctor` printed a `control box (where cloud boxes are built): e2b fresh` section while `doctor --json` contained no mention of a control box at all. Now `--json` carries a top-level `controlBox` block (`url`, `reachable`, and per provider `id`/`hasCredentials`/`configured`/`baseStatus`/`state`), absent when no control box is configured or it is this machine. The deliberate early return's concern is kept: the text and JSON forms read ONE function (`buildControlBoxInventory` in `control-plane/control-box-inventory.ts`, fed by `fetchControlBoxInventory` in `prepare.ts`), the read is bounded by the same 5s probe+deadline, and it can never fail the command — an unreachable box lands as `reachable: false` and a throwing probe as an absent block, with the exit code still reading `groups` only. | `apps/cli/src/commands/doctor.ts`, `apps/cli/src/control-plane/control-box-inventory.ts` | — |
| 9 | create-preflight latent false positive | The remote guard is `existsSync(workspace)`, not "is this the user's machine": a control box with a folder at the same absolute path runs the real `carry`/`model-auth` gates against the VPS's files and presents them as the user's. | `apps/hub/lib/hub-backend.ts` (`createPreflight`) | S |
| 10 | Credential fan-out fleet | Per-machine: a box only in the OTHER machine's `state.json` is never a target; custody mirroring (`recordInCustody`) is the remote-aware half. Best-effort by design. | `packages/relay/src/credentials-fanout.ts`, `apps/cli/src/commands/credentials.ts` | — |
| ~~12~~ | ~~A manager's `boxIds` on a PC hub with a remote store~~ | **Fixed in Phase 4** (2026-09-18): the attach is a `ManagerRecordStore.attachBox` behind `POST /api/v1/managers/{id}/attach-box`, and `attachJob` is now `attachManagerBox(managerId, target)`. The third and narrowest write a control box takes from another host. | `apps/hub/lib/backend/managers.ts`, `packages/relay/src/workspaces/manager-store.ts` | — |
| 11 | Persistent bring-back | Works (the owning hub's relay loop), but by omission: neither adopt path copies `persistent` onto the local record, so `agentbox ls` on the PC cannot show it. | `apps/cli/src/control-plane/hub-adopt.ts` (`hubBoxToRegistration`) | S |

| 13 | A manager's `boxJobIds` never promote to `boxIds` when the job is the PC's | The grouping lands (the enqueue sends `attachBox({boxJobId})`), but the promotion is `reconcileManagers`, which heals against the RECONCILING hub's own job list — the control box has no record of a `hub.mode=local` job, so it never promotes, and `manager list` counts the create as a pending job forever. The travelling patch is only ever called with `{boxJobId}`, never with `{boxId}` at completion, and the route takes "exactly one of the two ids" so it cannot express a swap. Decide which hub owns the promotion (the PC, at `completeCreateJob`, is the only one that knows) and let the attach carry both ids. Seen live: §F row F17, 2026-09-18. | `packages/relay/src/workspaces/manager.ts` (`reconcileManagers`), `apps/hub/lib/hub-backend.ts` (the `hub.create` wrapper), `apps/hub/app/(dashboard)/api/v1/managers/[id]/attach-box/route.ts` | M |
| 14 | A relay-executed `git.push` row carries no dedupe key | `recordBoxPushed` keys its row on the pushed tip (`push:<box>:<after>`), but that is the LEASED path only, and a control box in the default `gh` git-auth mode deliberately does not lease — the box routes through `recordBoxGitPush`, which passes no key. A second `agentbox-ctl git push` that prints `Everything up-to-date` therefore adds a second identical row. Either key the executor's row on the resulting tip too, or skip the row when nothing moved. Seen live: §F row F14, 2026-09-18. | `packages/relay/src/timeline-hooks.ts` (`recordBoxGitPush`), `packages/relay/src/host-actions.ts` | S |

| 15 | A docker box built under `hub.mode=local` is INVISIBLE to `list`/`ls` | **Won't fix — a consequence of an unsupported combination (2026-09-19).** `hub.mode` governs *gating* but not *targeting*. `dockerProvidersHidden` consults it, so `create` stops refusing and builds on the local hub (`withHubClient({preferLocal:true})`) — but `fetchBoxListing` resolves ONE target via `resolveHubTarget`, which never consults `hub.mode`, so the listing is fetched from the control box, which has never heard of the box. The box runs, `shell`/`cp`/`status`/lifecycle all work by box-ref, only the listing is empty. Live 2026-09-18 (§G row G1a). Reachable ONLY via `hub.mode=local` with a control box, which is **not supported** as of 2026-09-19: the refusals stopped recommending that mode (they point at a cloud provider or `agentbox remote-docker share <host>` instead) and every create/bake under it warns, naming this exact symptom. Reopen only if the combination is ever supported. | `apps/cli/src/control-plane/hub-list.ts` (`fetchBoxListing`), `apps/cli/src/commands/hub.ts` (`resolveHubTarget`), `apps/cli/src/commands/list.ts` | — |
| 16 | `hub unexpose` / `hub destroy` recommend a command that does not exist | Both refusals print ``Destroy them first (`agentbox hub boxes rm <id>`), or re-run with --force.`` but `agentbox hub boxes` has only `list` — `agentbox hub boxes rm <id>` answers `error: unknown command 'rm'`. The only real way out is the `--force` the same line offers. Pre-existing (`9d126eac`/`f4374f1b`, 2026-07-26, on `nightly`). Either add the subcommand or stop naming it. Live 2026-09-18 (§G). | `apps/cli/src/commands/control-plane.ts:2173` and `:2410` | S |
| 17 | `hub setup` cannot be run non-interactively | `AGENTBOX_HUB_ADMIN_EMAIL`/`_PASSWORD` DO short-circuit (`hub-auth-env.ts` reads `process.env` before prompting), but the `Copy this token to the control box?` confirm after `gh auth token` is unconditional and has no flag, so a scripted deploy hangs or dies on `TTY initialization failed`. §G had to drive it through the PTY harness. Wanted: a `--yes`/`--git-token`-style opt-in. Live 2026-09-18 (§G). | `apps/cli/src/commands/control-plane.ts:354` | S |
| 18 | A timeline row is silently lost between `workspace add` and a relay restart | `recordBoxGitPush` bails at `if (!ws) return;` when `timelineSink().workspaceFor()` finds nothing. A relay that was already running when `agentbox workspace add` registered the workspace on the control box keeps its old view, so pushes in that window record NOTHING — silently, because the push itself succeeds. After `agentbox relay restart` the very next push records normally. Live 2026-09-18 (§G row G9a). | `packages/relay/src/timeline-hooks.ts:95`, the relay's workspace lookup/caching | S |
| ~~19~~ | ~~**An in-box agent can push any branch with no approval** (security)~~ | **Closed by a model change** (2026-09-19), not by the fix it first got. The original report was correct *under the model of the day*: approval was keyed on the host-registered branch alone while the box's argv tail was appended to `git push <remote> <branch>`, so from a box on `agentbox/g1-2` both `agentbox-ctl git push g11-plain-branch` and `agentbox-ctl git push HEAD:refs/heads/g11-injected` reached GitHub with `hub approvals list` -> "No pending approvals" and `/admin/prompts?all=1` -> `{"prompts":[]}` — an argv hole in a branch-shaped gate. `82cceadd` closed that hole by requiring every ref the assembled command would write to be an already-sanctioned target. Then the model itself changed by decision: publishing commits is ordinary, revertable agent work (the same reasoning that keeps `gh pr merge` off `GH_DESTRUCTIVE`), so **a push to any branch now runs silently and "which branch" no longer gates anything**. What gates today is what a push *does*: a deletion (`--delete`/`-d`, `:branch`), a force-push to a branch that is not the box's own `agentbox/*` scratch branch (`--force`/`-f`, a `+` refspec), `--mirror`/`--prune`, a tag overwrite, `--repo`/`--receive-pack`/`--exec`, and any flag the gate does not recognise (fail closed). `--force-with-lease`/`--force-if-includes` stay silent. `pushDestructiveReason` (`packages/core/src/sync/git-refs.ts`) is the one decision both gate sites read; `git.lease-token` keeps its own scratch-only gate because the credential it hands out bypasses this one entirely. | `packages/relay/src/server.ts` (docker path), `packages/relay/src/host-actions.ts` (cloud path), `packages/core/src/sync/git-refs.ts` | M |
| 20 | A control box IGNORES a pushed `prepared/<provider>.json`, so the first cloud create fails | With the bake record pushed (`hub custody list` -> `prepared/e2b.json`, present on the VPS at `hub-data/hub/custody/prepared/e2b.json`), the E2B credential present (`/api/v1/providers` -> `hasCredentials: true`) and the template live on the account, the control box still reports `configured: false` / *"Credentials set — bake the base image to finish setup"*, and a hub-routed create dies at `no E2B base template found` after provisioning. Almost certainly the build-context fingerprint (`contextSha256`): the VPS's own tree fingerprints differently from the PC's, so the pushed record is "not for this build context" — the same wording `hub expose` prints ("neither side has a e2b base for this build context"). Remedy today is a bake ON the control box, which then reports `[build] Template created with ID: o05kawibx9vcmxvgjnk4` — **the same id the pushed record already named**, i.e. the artifact was never missing, only the record of it. Live 2026-09-18 (§G row G12a). | the prepared-state resolution behind `/api/v1/providers` + the create worker's base lookup; `packages/sandbox-core` prepared-state | M |

| 21 | A hub-routed service-agent box comes up with NO channel tokens | **The bot is mute.** It starts, the gateway is reachable, `agentbox openclaw` prints its URL — and it cannot answer on Telegram or Discord, because `~/.openclaw/.env` was never written. **Cause:** `withPerBoxCarry` runs INSIDE `provider.create` (`packages/sandbox-core/src/per-box-carry.ts` — `perBoxCarrySrc` resolves `~/.agentbox/<agent>/<box>.env` against the running machine's `homedir()`), so on a hub-routed create it looks on the VPS. The openclaw spec's carry entry is `optional: true` on a first create, so nothing fails: it logs `no <path>` and continues. **Why it is not a one-liner:** the file is keyed by BOX NAME and the hub mints that name during the create, so the PC cannot resolve the file before it knows the name — that chicken-and-egg IS the problem. Options seen, none chosen: (a) the client sends the name up front (`--name` is already the explicit case; the hard case is the derived one, which the PC could compute and send as `name`); (b) the worker renders it from a placeholder, i.e. the PC pushes the file into custody under a name-shaped key and the worker resolves it after minting; (c) move per-box carry resolution OUT of `provider.create` to the machine that holds the file, handing the resolved entry down the existing `carry` list — which is how every other host file already reaches a hub-built box (`pushCreateSeed`). (c) is the same shape as the `carry:` fix and is probably the right one. **Same defect as item 1** (a credential resolved on the wrong machine), and the create-side twin of item 5 — but unlike item 1 the bytes do NOT already reach the control box, so it is not "ship the selection". Made more visible by the item 2 fix: openclaw creates now actually reach the control box. | `packages/sandbox-core/src/per-box-carry.ts`, `apps/cli/src/control-plane/create-target.ts` (`pushCreateSeed`), `apps/hub/lib/hub-worker.ts` | M |
| 22 | An `external` manager on a PC has no heartbeat poster while the PC hub is down | **Its liveness is a guess for 30 minutes.** The heartbeat loop that tells a control box what a manager is doing runs only inside the PC's hub daemon (`apps/hub/server.ts` → `startManagerHeartbeats`, started only when the record store is remote). Nothing on a manager READ path starts that daemon: `resolveHubApiTarget` autostarts a hub only for `mode: local`, and with a control box configured `manager list/status/note/forget`, `tasks add`, `create` and the tray all resolve remote. A plain claude/codex session in your terminal — the common kind — therefore usually reports nothing at all, and the control box renders it from `lastSeenAt` alone: `running` for 30 minutes after its last `agentbox` call, `stopped` after, whatever the truth. `manager start` is the only path that incidentally brings the loop up. Mitigated 2026-09-19 (a running heartbeat now advances `lastSeenAt`, so the window measures the last sign of life rather than the moment the record was created, and `managerStatus` prefers a fresh report over the window), but making it TRUTHFUL needs the PC's hub to be running — which is a decision about what a PC still runs once it has a control box, not a bug fix. | `apps/hub/lib/manager-heartbeat.ts`, `apps/hub/server.ts:230`, `apps/cli/src/commands/control-plane.ts:1281` | M |
| 23 | A hostname flip blocks resume/attach of a session that is right here | **Trust the socket, not the hostname.** Hit live 2026-09-23: `agentbox manager resume` answered `manager b3f65baf18635b12 runs on Marcos-MacBook-Pro.local, not on Marcos-MacBook-Pro-2.local; resume it there`, and hours later the name had flipped BACK on its own — macOS derives `gethostname()` from `LocalHostName` when `HostName` is unset, and auto-suffixes it `-2` on a Bonjour/LAN name collision. An intermittent identity, not a rename. A pty-hosted session is addressed purely by local path — `~/.agentbox/pty/<managerId>.{sock,json}`, and `PtySessionMeta` carries no host field — so **a socket in the caller's own `$HOME` that answers `ptyHostAlive` is proof of locality, strictly stronger than the recorded string.** The code already holds that evidence and throws it away: `runsHere(m)` is tested BEFORE `readPtyMeta(m.id)` (`apps/cli/src/commands/manager.ts:313`), and the hub refuses earlier still via `storeIsLocal` → `elsewhere()` (`apps/hub/lib/backend/managers.ts:267`, callers at :1057 resume, :1079 attach, :1120 pin, :1244 message), while `managerStatus` will not even dial the socket unless `rec.host === host` (`packages/relay/src/workspaces/manager.ts:534`). **Fix (S):** for a `kind: 'pty'` record, probe the socket first; when it answers, treat the session as local, serve resume/attach/stop/message, and re-stamp `host` to the current hostname so the record self-heals — the same rewrite a detect already does for a running external session (`manager.ts:788`). Keep the hostname refusal only as the fallback for a record whose socket is absent, and for tmux/external kinds. Tests to update: `apps/hub/test/backend-managers.test.ts:347,749,1021`, `apps/cli/test/manager-host-routing.test.ts`, `packages/relay/test/manager.test.ts:662`. **Out of scope here, deliberately:** the wider identity work (a stable `~/.agentbox/machine-id` replacing `os.hostname()` across manager records, workspace `hosts` maps and timeline rows — ~63 call sites) belongs to Phase 1 of `~/Projects/AgentBox/docs/multi-host-and-teams-plan.md`, which needs it for multi-PC and multi-user anyway. The socket fix removes the sharp edge a single user hits today; until the rest lands, a flip still refuses a new `manager start`, `workspace rescan`, `-w <path>` and `--tasks` resolution, and a detect under the new name mints a duplicate workspace. Workarounds: `sudo scutil --set HostName <name>` to pin the name, `workspace add <root>` to re-point folders, `manager forget --force` as the escape hatch. | `apps/cli/src/commands/manager.ts` (`runsHere` at :175, the attach path at :313), `apps/hub/lib/backend/managers.ts` (`storeIsLocal` :198, `elsewhere` :267), `packages/relay/src/workspaces/manager.ts` (`managerStatus` :534, the detect rewrite :788), `packages/sandbox-core/src/pty-session.ts` | S |
| 24 | A host egress-IP change locks every VPS box until something fails | **Nothing watches the IP; only two paths self-heal, and both by failing first.** The per-box Hetzner/DigitalOcean firewall is written at create from the egress IP detected then, and re-synced only by an explicit `agentbox <provider> firewall sync` / `inbound`, or by `withFirewallRepair` (`apps/cli/src/lib/firewall-repair.ts`) — wired at exactly two ESTABLISH sites: `agentbox recover` and the first connect of a cloud attach. Everything else fails with no repair: `shell`/`status`/`logs`/`cp`, host-side git ops, the hub worker and preview poller, and — the direction no CLI wrapper can reach — the host relay's box→host actions, since a box's `git push` rides the host's SSH ControlMaster, which the new IP cannot re-establish. Hit live 2026-09-23 after a network change: a Claude session in a hetzner controlclaw box was stuck until `agentbox hetzner firewall sync` was run by hand. The probe is off the happy path on purpose (`egressIpCached`, `firewallEgressStatus` in `packages/sandbox-hetzner/src/backend.ts:274`), so the host never re-checks until something breaks. **Fix:** let the relay own it — cache the last egress IP, re-detect on relay start and on a slow timer (or a network-change event), and on a change call `provider.repairReachability` once for every running hetzner/digitalocean box. One probe per interval instead of per command, and it repairs the box→host direction too. **Gate it on the fleet, not on a timer:** a relay with no boxes on a firewall-bearing provider (today hetzner and digitalocean; docker, daytona, vercel, e2b and remote-docker have no per-box firewall) must do nothing at all — no egress probe, no provider API call. Read `state.json` first, run the probe only when at least one such box is `running`, and stop the loop when the last one is destroyed, so an idle laptop and a docker-only user never see the traffic. Wrapping the remaining establish sites (exec/shell/git) is the cheaper half-measure. Relevant to multi-PC: with several devices the allowed set becomes per-device, and with the hub relaying (Stage 1 Phase 2 of the multi-host plan) a box's firewall should admit the hub, not each PC. | `apps/cli/src/lib/firewall-repair.ts`, `apps/cli/src/commands/recover.ts`, `apps/cli/src/commands/_cloud-attach.ts`, `packages/sandbox-hetzner/src/backend.ts` (`repairReachability`, `firewallEgressStatus`), `packages/sandbox-digitalocean/src/backend.ts`, `packages/relay/src` (no watcher today) | M |

Nothing new was found in Phase 4: the two enrichment paths it changed (PR sync, push +/-) were the
last host-checkout assumptions in the workspace layer, and item 12 was its own last open item.
Items 13 and 14 came out of the §F run against a real control box (2026-09-18); two other findings
from that run were fixed on the spot (a local hub inheriting the control box's auth profile, and a
stale local workspace record shadowing the control box's on the create-row join).

Item 22 came out of the manager audit behind the tray work (2026-09-19). Four findings from that
audit were fixed rather than listed, because each made a client render a manager wrongly: the
workspace row's `managers.running` count ignored the heartbeat and contradicted `GET /managers` for
half an hour; `lastSeenAt` was never advanced by a heartbeat, freezing the fallback clock at detect
time; a `wrong_host` about a workspace named an arbitrary one of its machines, so a client on any
other one skipped the retry; and a hub whose records live on a control box repeated that box's
heartbeat-derived status for managers running on its OWN machine, which sent `manager attach` to
`resume` and `resume` back to attach.

Items 13 and 4 are the two this backlog now knows the tray surfaces: a create that never promotes
`boxJobIds` shows as a pending job forever in the Manager window's box list, and a hub-created cloud
box reporting `hasGit: false` hides its whole Git submenu in the tray and the web UI.

Works remote as designed, no action: create-preflight's explicit `unavailable` (item 9 aside),
`agentbox url` / `agentbox-ctl open` / `GET /boxes/{id}/web`, the box size picker, `agentbox clone`
routing, `manager attach`'s refusal.

## §G general regression pass (2026-09-18, real Hetzner control box at `116.203.220.73.sslip.io`)

Items **15-20** above came out of it. It also **live-verified items 1, 2, 3, 8 and 14** (each had been
documented but not confirmed against a two-machine deployment) and **corrected items 4 and 5**.

What was confirmed WORKING remote, with a real control box and no laptop hub:
`create` (docker, via `hub.mode=local` — a shape that is no longer supported, see items 15 and 3) · lifecycle stop/start/pause/unpause · `status` ·
`checkpoint create/ls` · `url` + `GET /boxes/{id}/web` (resolves on the PC, 404 on the VPS, correctly) ·
`upload`/`download` (correctly NOT refused for a docker box — `remoteHubHostFileRefusal` keys on
`onThisMachine`) · `cp` both directions · `shell` · `--persistent` incl. the destroy guard ·
`hub credentials push` + `custody list` · `hub project push` · a hub-routed e2b create whose agent answers a
real prompt · **`agentbox-ctl git push` from a cloud box with the PC's hub AND relay stopped** (the whole point
of a control box) · the tray-facing payloads on `/api/v1/boxes`, `/api/v1/managers` (`hostIsHub: false` correct
across two machines) and `…/timeline` (`lane` + `branchUrl` on every row; `summary` only with `?since=`, by design).

**No regression attributable to `wip/remote-hub-p2-4` was found.** Every failure traced to a commit already on
`nightly`; the branch does not touch `git.ts`, `list.ts`, `hub-list.ts` or `remote-hub.ts` at all, and its edits
next to the push gate only extract `decideHostInitiatedPush` without changing how the bypass is computed.
