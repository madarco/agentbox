# Agent instructions

This file is part of the **openclaw workspace**: `/workspace` inside the box is
what the gateway's agent reads and writes, so everything here — plus `skills/`
and anything the agent writes back — is the agent's working memory.

Keep answers short. Prefer doing the thing over describing it. When a task
needs a tool that is not available, say so plainly rather than approximating.
