{
  "name": "@agentbox/agent-modules",
  "version": "0.0.0",
  "private": true,
  "description": "Registers every built-in agent's behavior \u2014 the one place an app wires agents in",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@agentbox/agent-codex": "workspace:*",
    "@agentbox/agent-opencode": "workspace:*",
    "@agentbox/sandbox-docker": "workspace:*"
  },
  "devDependencies": {
    "@agentbox/agent-registry": "workspace:*",
    "@types/node": "^22.10.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
