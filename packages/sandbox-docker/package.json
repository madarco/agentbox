{
  "name": "@agentbox/sandbox-docker",
  "version": "0.0.0",
  "private": true,
  "description": "Local Docker sandbox provider for AgentBox",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "Dockerfile.box"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint src test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@agentbox/config": "workspace:*",
    "@agentbox/core": "workspace:*",
    "@agentbox/ctl": "workspace:*",
    "@agentbox/relay": "workspace:*",
    "@agentbox/sandbox-core": "workspace:*",
    "execa": "^9.5.2",
    "smol-toml": "^1.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
